apiVersion: v1
kind: Service
metadata:
  name: {{ include "ordering-app.fullname" . }}
  labels:
    {{- include "ordering-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ordering-app.selectorLabels" . | nindent 4 }}
